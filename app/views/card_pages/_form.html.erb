<%= form_for(@card_page) do |f| %>
  <% if @card_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_page.errors.count, "error") %> prohibited this card_page from being saved:</h2>

      <ul>
      <% @card_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
